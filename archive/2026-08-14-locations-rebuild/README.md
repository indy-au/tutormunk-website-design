# Locations rebuild archive (14 Aug 2026)

This folder holds files superseded by the Locations system rebuild (real
business data: TutorMunk Gregory Hills, 23 suburbs, one page per suburb at
/tutoring-in-[suburb-slug]).

- `locations.oran-park.tsx.old`: the single hardcoded suburb route that every
  suburb card on the old Locations page linked to (the routing bug). Replaced
  by 23 individual route files, including a real /tutoring-in-oran-park page.

Kept per CLAUDE.md rule 5 (never delete without approval, archive instead).
